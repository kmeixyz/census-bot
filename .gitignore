# dependencies
/node_modules
/.pnp
.pnp.js

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files — NEVER commit this
.env.local
.env.development.local
.env.test.local
.env.production.local

# raw ACS source files — regeneratable via `npm run fetch:acs-docs` and
# `npm run fetch:acs-tables`. Only the indexed JSON files (acs-data/index.json,
# acs-data/tables-index.json) are checked in.
acs-data/raw/

# vercel
.vercel
.claude/worktrees/
.claude/settings.local.json
